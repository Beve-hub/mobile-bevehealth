import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const CustomCall = (props: Props) => {
  return (
    <View>
      <Text>CustomCall</Text>
    </View>
  )
}

export default CustomCall

const styles = StyleSheet.create({})