import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const CustomDocument = (props: Props) => {
  return (
    <View>
      <Text>CustomDocument</Text>
    </View>
  )
}

export default CustomDocument

const styles = StyleSheet.create({})